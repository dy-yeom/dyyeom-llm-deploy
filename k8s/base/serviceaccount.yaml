apiVersion: v1
kind: ServiceAccount
metadata:
  name: wins-assist-sa
  namespace: wins-assist
  annotations:
    # IRSA: EKS Pod에서 AWS 서비스 접근용 IAM Role
    eks.amazonaws.com/role-arn: ROLE_ARN_PLACEHOLDER
